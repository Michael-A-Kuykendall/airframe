--- src/tools/image.rs
+++ src/tools/image.rs
@@ -24,19 +24,20 @@
     async fn execute(&self, args: ToolArgs) -> Result<ToolResult, ToolError> {
         // Extract parameters
         let path_str = args.args.get("path")
-            .ok_or_else(|| ToolError::MissingArgument("'path' parameter is required".to_string()))?;
+            .and_then(|v| v.as_str())
+            .ok_or_else(|| ToolError::MissingArgument("path".to_string()))?;
         
         let mode = args.args.get("mode")
-            .map(|s| s.as_str())
+            .and_then(|s| s.as_str())
             .unwrap_or("base64");
 
         // Validate mode
         if !["base64", "meta", "ocr"].contains(&mode) {
-            return Err(ToolError::InvalidParameters(
-                format!("Invalid mode '{}'. Must be 'base64', 'meta', or 'ocr'", mode)
-            ));
+            return Err(ToolError::InvalidArgument(
+                "mode".to_string(),
+                format!("Invalid mode '{}'. Must be 'base64', 'meta', or 'ocr'", mode)
+            ));
         }
