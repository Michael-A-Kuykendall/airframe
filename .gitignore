/target
crates/devkit/
.agents/
.beads/
.beads
.claude/
.opencode/
.serena/
.serena
.vscode/
.vscode
AGENTS.md
bin/
recovery/
.mcp/
/*.py
/*.sh
/*.txt
*.log
*.pyc
__pycache__/
**/__pycache__/
*.stackdump
*.bak
fixtures/hellaswag_val.jsonl
fixtures/arc_challenge_test.jsonl
fixtures/arc_easy_test.jsonl
fixtures/lambada_test.jsonl
.beads/daemon-error
.beads/export-state/
crates/console/compile_errors*.txt
crates/console/errors_hist*.txt
crates/console/fix_image.patch
vendor/shimmy/
vendor/human-eval/

# Local environment
.venv/

# bd / beads issue tracker — local Dolt DB stays LOCAL (sync via bd dolt push/pull
# to the private remote); only .beads/config.yaml + .beads/metadata.json are tracked.
.beads/embeddeddolt/
.beads/backup/
.beads/bodyfix/
.beads/plans/
.beads/reference/
.beads/daemon-error
.beads/export-state/
.beads/*.jsonl
.beads/*.db
.beads/*.meta.json
.beads/daemon.*
.beads/.auto-import-issues.jsonl
.beads/.local_version
.beads/interactions.jsonl
.beads/last-touched
.beads/sync-state.json

# Smoke test run artifacts (timestamped CSVs and logs)
artifacts/model_smoke/smoke_*.csv
artifacts/model_smoke/smoke_*.log

# Generated debug traces / probe outputs (local analysis artifacts)
artifacts/debug/

# Internal spike / planning docs (never commit to public repo)
SPIKE_*.md
VISION_OPTION_*.md
docs/vision-task-breakdown.md

# Vision work-in-progress (unshipped, kept local until branch is ready)
src/family/resampler.rs
src/family/vit.rs
src/ops/reference/vision.rs
tests/vision_math.rs

# Internal libfse docs (patent/strategy materials)
crates/libfse/STRATEGY.md
crates/libfse/AUDIT_INFO.md
crates/libfse/docs/PROMPT_FOR_LIBFSE_GENERATION.md
crates/libfse/docs/NAMING_CONVENTION_MILITARY.md
crates/libfse/docs/COMMERCIAL_VALUE_PROP.md
crates/libfse/docs/patent_materials/
fused_semantic_execution_full_markdown_reconstruction.md
# Development hygiene
artifacts/
docs/chat*.md
.github/copilot-instructions.md
.github/instructions/
crates/libfse/docs/

# Internal developer docs — never committed (session notes, AI handoffs, audits)
docs/internal/

# vault/seeds/  # Now committed as canonical vault data
# vault/vault.duckdb IS committed (via LFS) — do NOT add vault/ here

# Diagnostic / probe output files (large JSON dumps, local-only)
capital
layer_dump_*.json
layer_dump_pos*.json
fc_qwen3_*.json
gpu_qwen3_*.json
compare_qwen3_*.json
fc_run_*.log
sanity_out.txt

# Coverage / test reports (generated locally, never commit)
tarpaulin-report.html
cobertura.xml

# Artifacts from cert runs (timestamped, local-only)
artifacts/battery_int4_*.txt

# Windows reserved device name artifact (from bad command redirect)
nul
.aider*

# Root-level internal dev docs — never committed
internal/

# VS Code chat chronicle
.vscode/chat-chronicle.json

# Scratch files
console-functional-review.md
.qwen/

# Build artifacts and cache
build/artifact/*
target/*.rldb
target/debug/**/*
target/release/[a-z0-9]*all_sessions_export.json
.opencode/opencode.json

# opencode session logs (not part of hotfix release changes)
session.json
session_ses_*.json

# candle_probe build output — never commit (large .rlib files exceed GitHub 100MB limit)
crates/candle_probe/target/

# Local cert audit databases — never commit
*.sqlite
*.duckdb
*.duckdb-shm
*.duckdb-wal
beads.db

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Dev-only / local-only (guarded from public pushes by pre-push hook)
.serena/
.cargo/
*.bak
docs/SPIKE_*.md
SPIKE_*.md

# AI-assisted development files — private side only, never push to public
.github/copilot-instructions.md
.github/instructions/
