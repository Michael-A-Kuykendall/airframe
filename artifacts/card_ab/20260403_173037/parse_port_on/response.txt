use rusoto_core::{Client, Region};
use rusoto_dynamodb::{DynamoDbClient, Table};

fn main() {
    let client = DynamoDbClient::default().unwrap();
    let table = Table::new(client, "port_table").unwrap();

    let mut parser = parse_port::<_, _, _>(table);
    parser.run();
}

// END_RUST_FILE