use rusoto_core::{client::Client, config::Config};
use rusoto_dynamodb::{DynamoDbClient, Table};
use rusoto_lambda::{LambdaClient, Lambda};
use rusoto_s3::S3Client;
use rusoto_sts::{StsClient, Sts};
use rusoto_types::{error::RustCodeError, function::{Function, FunctionInput, FunctionOutput}, function_input::{Input, Output}, function_output::{Output, OutputKind, OutputKind::*}, function_output_input::{Input, OutputInput}, function_output_output::OutputOutput};
use serde::{Deserialize, Serialize};
use std::env;
use std::fs::File;
use std::io::{self, BufRead, BufReader, BufWriter, Write};
use std::path::Path;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let config = Config::builder()
        .region("us-east-1")
        .build()?;

    let dynamodb = DynamoDbClient::builder()
        .region(config.region())
        .build()?;

    let lambda = LambdaClient::builder()
        .region(config.